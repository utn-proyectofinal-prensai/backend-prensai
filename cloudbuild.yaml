# Google Cloud Build configuration for automated deployment
# This file is used by Cloud Build to build and deploy the application to Cloud Run

steps:
  # Build the container image with BuildKit enabled
  - name: 'gcr.io/cloud-builders/docker'
    env:
      - 'DOCKER_BUILDKIT=1'
    args: 
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/prensai-api:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/prensai-api:latest'
      - '.'
    timeout: 1800s

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/prensai-api:$COMMIT_SHA'

  # Push the latest tag
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/prensai-api:latest'

  # Run database migrations
  - name: 'gcr.io/google-appengine/exec-wrapper'
    secretEnv:
      - RAILS_MASTER_KEY
      - DATABASE_URL
    args:
      - '-i'
      - 'gcr.io/$PROJECT_ID/prensai-api:$COMMIT_SHA'
      - '-s'
      - '${_CLOUDSQL_INSTANCE_CONNECTION_NAME}'
      - '-e'
      - 'RAILS_ENV=production'
      - '-e'
      - 'RAILS_MASTER_KEY'
      - '-e'
      - 'DATABASE_URL'
      - '--'
      - 'bundle'
      - 'exec'
      - 'rails'
      - 'db:migrate'

  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'prensai-api'
      - '--image'
      - 'gcr.io/$PROJECT_ID/prensai-api:$COMMIT_SHA'
      - '--region'
      - '${_CLOUD_RUN_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--add-cloudsql-instances'
      - '${_CLOUDSQL_INSTANCE_CONNECTION_NAME}'
      - '--set-env-vars'
      - 'RAILS_ENV=production,RAILS_LOG_TO_STDOUT=true,AI_MODULE_BASE_URL=${_AI_MODULE_BASE_URL},GCS_PROJECT_ID=${_GCS_PROJECT_ID},GCS_BUCKET_NAME=${_GCS_BUCKET_NAME}'
      - '--set-secrets'
      - 'RAILS_MASTER_KEY=rails-master-key:latest,DATABASE_URL=database-url:latest,GCS_CREDENTIALS_JSON=gcs-credentials:latest'
      - '--memory'
      - '${_MEMORY}'
      - '--cpu'
      - '${_CPU}'
      - '--min-instances'
      - '${_MIN_INSTANCES}'
      - '--max-instances'
      - '${_MAX_INSTANCES}'
      - '--no-cpu-throttling'
      - '--timeout'
      - '300'
      - '--concurrency'
      - '80'

# Substitutions for environment-specific values
substitutions:
  _CLOUD_RUN_REGION: us-central1
  _CLOUDSQL_INSTANCE_CONNECTION_NAME: ${PROJECT_ID}:us-central1:prensai-db
  _MEMORY: 1Gi
  _CPU: '1'
  _MIN_INSTANCES: '1'
  _MAX_INSTANCES: '2'
  _AI_MODULE_BASE_URL: ''
  _GCS_PROJECT_ID: ${PROJECT_ID}
  _GCS_BUCKET_NAME: ${PROJECT_ID}-prensai-storage

# Images to be pushed to Container Registry
images:
  - 'gcr.io/$PROJECT_ID/prensai-api:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/prensai-api:latest'

# Build timeout
timeout: 2400s

# Use Kaniko cache for faster builds
options:
  machineType: 'N1_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/rails-master-key/versions/latest
      env: RAILS_MASTER_KEY
    - versionName: projects/$PROJECT_ID/secrets/database-url/versions/latest
      env: DATABASE_URL
